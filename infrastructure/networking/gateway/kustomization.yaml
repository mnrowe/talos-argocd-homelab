apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

resources:

  # Gateway resources
  - ns.yaml
  #- gw-external.yaml
  - gw-internal.yaml
  - certificate.yaml


generatorOptions:
  disableNameSuffixHash: true
