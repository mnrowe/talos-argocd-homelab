apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: serverops-live-wildcard
  namespace: gateway
spec:
  secretName: serverops-live-tls
  issuerRef:
    name: cloudflare-cluster-issuer
    kind: ClusterIssuer
  dnsNames:
  - "*.serverops.live"
  - "serverops.live"
