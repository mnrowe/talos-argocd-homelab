apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: qbt-config
  namespace: qbt
  labels:
    backup: "daily"
spec:
  accessModes:
    - ReadWriteOnce
  storageClassName: longhorn
  resources:
    requests:
      storage: 5Gi
